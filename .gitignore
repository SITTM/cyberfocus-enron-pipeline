# Corpus — NEVER commit. Lives at /home/jez/data/enron/
data/
maildir/
*.tar.gz

# Databases and derived artefacts
*.db
*.sqlite
*.sqlite3
*.parquet

# Annotation working files (contain personal data). Everything in here is
# excluded regardless of extension -- the per-reviewer sheets are .csv but the
# split manifest is .json, and an extension-based rule silently missed it.
# The repository is PUBLIC; a stray `git add annotations/` must not publish
# real people's email addresses.
annotations/*
!annotations/template.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/
*.egg-info/

# OS / editor
.DS_Store
.idea/
.vscode/

db/
